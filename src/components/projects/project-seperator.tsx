/** @jsx jsx */
import { Box, Flex, jsx } from "theme-ui";
import React from "react";
import Logo from "../logo";
import Fade from "react-reveal/Fade";
export type ProjectSeperatorProps = {};
export const ProjectSeperator: React.FC<ProjectSeperatorProps> = ({}) => {
  return (
    <Flex
      sx={{
        width: "100%",
        justifyContent: "center",
        position: "relative",
        height: "0px",
      }}
    >
     
     
    </Flex>
  );
};

export default ProjectSeperator;
